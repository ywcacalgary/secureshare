# secureshare
Secure Share is a single-file one-time secret web app that lets users create expiring, single-use links or QR codes to safely share sensitive credentials. Built with HTML, JavaScript, Tailwind CSS, and Supabase, it includes token hashing, audit fields, countdown expiration, and a reveal-once recipient flow.
